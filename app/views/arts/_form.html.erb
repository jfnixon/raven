<%= form_for(@art) do |f| %>
  <% if @art.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@art.errors.count, "error") %> prohibited this art from being saved:</h2>

      <ul>
      <% @art.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :yubikey %><br />
    <%= f.text_area :yubikey %>
  </div>
  <div class="field">
    <%= f.label :le_count %><br />
    <%= f.text_field :le_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
