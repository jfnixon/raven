<%= form_for(@artwork) do |f| %>
  <% if @artwork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@art.errors.count, "error") %> prohibited this art from being saved:</h2>

      <ul>
      <% @art.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :yubikey %><br />
    <%= f.text_field :yubikey %>
  </div>
  <div class="field">
    <%= f.label :work_number %><br />
    <%= f.text_field :work_number %>
  </div>
  <fieldset><legend>Admin use only</legend>
  <div class="field">
    <%= f.label :owner_id%> <span class="admin">(admin)</span><br />
    <%= f.text_field :user_id %>
  </div>  
  <div class="field">
    <%= f.label :book_id %> <span class="admin">(admin)</span><br />
    <%= f.text_field :book_id %>
  </div>
  </fieldset>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
